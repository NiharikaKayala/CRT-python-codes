def count(string):
    string=string.lower()
    v=0
    c=0
    for ch in string:
        if (ch == "a" or ch == "e" or ch == "i" or  ch == "o" or ch == "u"):
          v+=1
        else:
          c+=1
    return[v,c]
print(count("niha"))